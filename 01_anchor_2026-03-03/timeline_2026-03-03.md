# Timeline - 2026-03-03

## Purpose

This file summarizes the public timeline for the 2026-03-03 artifact anchor.

This date is treated as the first artifact anchor.

It is not the Apple Support contact date.

---

## Summary

2026-03-03 shows a visible-management-absent artifact state combined with FileProvider / SaveToFiles activity, iCloud Drive not-configured state, AppleAccount / Cloud / Search / Trial / Delete process context, and storage / memory pressure.

The key point is not classic MDM.

The key point is:

Visible MDM / supervision / profile indicators were not observed, while internal restriction and FileProvider/account-related structures were present.

---

## Key artifact timeline

### Early day

SiriSearchFeedback / Search-related activity appears repeatedly.

This includes parsecd / spotlight / photos / visualintelligence-related feedback activity.

Interpretation:

This establishes Search / Siri / Spotlight-related activity on the date, but it is not by itself the main anchor.

---

### 11:53 VN

First reviewed sysdiagnose anchor.

Relevant findings:

* MCState present
* ManagedSettings present
* `IsSupervised:false`
* `PostSetupProfileWasInstalled:false`
* `MCProfileEvents: []`
* visible profile / payload structures empty
* EffectiveUserSettings present
* ScreenTime / ManagedSettings local stores present
* Safari private browsing / history clearing restrictions not yet enabled

Interpretation:

This is the first strong visible-management-absent artifact point.

---

### 12:14 VN

Second reviewed sysdiagnose anchor.

Relevant findings:

* MCState / ManagedSettings state remained consistent with the 11:53 anchor
* visible MDM / supervision / profile indicators still absent
* ManagedSettings / ScreenTime local stores still present

Interpretation:

This supports state persistence across the mid-day sysdiagnose window.

---

### 12:27 VN

`ospredictiond` CPU resource activity appears.

Relevant context:

* prediction-related daemon activity
* CPU resource context
* storage pressure context

Interpretation:

This is supporting context only.

It is not treated as the core restriction event.

---

### 12:51 VN

`triald` proactive event activity appears.

Relevant context:

* Trial / asset / factor activity
* localization / carrier context observed in the reviewed logs
* Siri / text-to-speech / factor-related context

Interpretation:

Trial activity is relevant because similar daemon clusters recur across the reviewed dates.

---

### 16:34 VN

`ospredictiond` CPU resource activity appears again.

Relevant context:

* CPU resource event
* thermal / resource context
* storage pressure context

Interpretation:

This contributes to the day’s resource-pressure pattern.

---

### 16:48 VN

`signpost_reporter` CPU resource context appears.

Relevant process context included:

* `cloudd`
* `deleted`
* `searchd`
* `triald`
* `AppleAccount`
* `Accounts`

Interpretation:

This is one of the important process-cluster observations for the date.

The cluster connects cloud/account/search/delete/trial activity in the same general reporting context.

---

### 20:05-20:09 VN

Stackshot sequence and sysdiagnose context.

Relevant process context included:

* `fileproviderd`
* `LocalStorageFileProvider`
* `SaveToFiles`
* `CloudDocs`
* `cloudd`
* `bird`
* `deleted`
* `searchd`
* `triald`
* `parsecd`
* `suggestd`
* `duetexpertd`
* `appleaccountd`
* `accountsd`
* `CommCenter`
* `corespeechd`
* message / AI / translation-related app context in stack or disk-write context

Interpretation:

This period shows the beginning of the stronger evening FileProvider / cloud/account/search/delete cluster.

---

### 20:51 VN

Reviewed sysdiagnose anchor.

Relevant findings:

* MCState present again
* visible MDM / supervision / profile indicators still absent
* EffectiveUserSettings consistent with earlier reviewed state
* ManagedSettings present
* FileProvider context present
* iCloud Drive FileProvider provider state present as enabled / needs-auth
* `brctl` still reports iCloud Drive as logged out / not configured

Interpretation:

This confirms that the visible-management-absent state persisted into the evening while FileProvider / iCloud Drive provider state remained relevant.

---

### 20:59 VN

Large `fileproviderd` disk-write event.

Relevant observation:

* approximately 1GB-class disk-write activity
* low free storage context
* FileProvider / SaveToFiles / app-on-behalf-of context

Interpretation:

This is the main storage-pressure event for 2026-03-03.

It becomes important when compared with:

* 2026-03-04 multiple 1GB-class disk-write events
* 2026-03-05 large WeChat disk-write event

---

### 21:34 VN

Final reviewed sysdiagnose / stackshot anchor for the date.

Relevant findings:

* MCState still present
* visible MDM / supervision / profile indicators still absent
* ManagedSettings present
* FileProvider indexing / provider context present
* Wi-Fi off / WWAN context observed
* memory pressure reached the strongest reviewed point for the date

Interpretation:

This is the endpoint of the 2026-03-03 artifact anchor.

It shows the same visible-management-absent condition while memory / storage pressure and FileProvider/account/cloud process context were still present.

---

## Day-level interpretation

2026-03-03 should be interpreted as:

Visible-management-absent artifact state
plus
ManagedSettings / MCState presence
plus
FileProvider / iCloud Drive needs-auth context
plus
storage / memory-pressure event.

It should not be described as the Apple Support contact date.

---

## Normal interpretation

Possible normal explanations include:

* low storage
* FileProvider / SaveToFiles background behavior
* iCloud Drive not configured but provider state still retained
* ordinary iOS background daemon activity
* normal Analytics / Trial / Search / Cloud processing
* memory pressure caused by ordinary app and system workload

---

## Anomalous interpretation

The date remains relevant because:

* visible MDM / supervision / profile indicators were absent
* internal restriction-evaluation structures existed
* FileProvider / iCloud Drive provider state persisted while `brctl` reported iCloud Drive not configured
* FileProvider / storage pressure became severe
* Cloud / Account / Search / Delete / Trial process context appeared together
* the pattern reappeared or developed further on 2026-03-04 and 2026-03-05

---

## Boundary

This timeline does not prove:

* malware
* actor attribution
* Evil Twin / Rogue AP use
* malicious profile injection
* classic MDM enrollment
* Apple-side causation
* Apple Support interaction on this date

This file only preserves the public technical timeline for the 2026-03-03 artifact anchor.
